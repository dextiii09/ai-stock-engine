[pytest]
; IV&V finding 2026-08-21 (audit Finding #7): a bare `pytest` invocation
; from the project root previously collected across the whole repo,
; including live-network dev scripts and .venv's own vendored test suites
; (numpy, etc.) — extremely slow and non-deterministic, not a meaningful
; pass/fail signal. Scope collection to the real unit test suite.
testpaths = backend/tests
